{
  "name": "quiz-admin",
  "version": "1.0.0",
  "description": "Quiz Admin Panel - Full Stack Application",
  "scripts": {
    "client": "cd frontend && npm start",
    "server": "cd backend && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run install-all && cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install-all": "npm install && cd frontend && npm install --include=dev && cd ../backend && npm install"
  },
  "keywords": [
    "quiz",
    "admin",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}